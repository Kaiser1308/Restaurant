# Nhiệm vụ đang làm (Active Tasks)

Dùng để xé nhỏ `PHASE_1_TASKS.md` thành các việc chi tiết của ngày hôm nay.

## Task: Day 2 - Base Components & Backend Config
**Status:** ✅ Done

### Yêu cầu
setup Injection bên Backend.

### Checklist (Để giao cho AI)
- [x] **Frontend:** Scaffold base components: Button, Input, Modal, Card, StatusBadge, EmptyState, LoadingSpinner, Toast.
- [x] **Frontend:** Create layouts: WaiterLayout (mobile, bottom nav), CashierLayout (desktop, sidebar), OwnerLayout (desktop, full sidebar).
- [x] **Frontend:** Setup API client service (axios instance with interceptors for JWT token).
- [x] **Frontend:** Create feature folder structure for: auth, tables, menu, orders, bills, audit, reports.
- [x] **Backend:** Setup Dependency Injection ở `Program.cs`.
- [x] **Backend:** Thêm pattern `Result<T>` để chuẩn hóa API response.

Commit: `chore: scaffold base components, layouts, and project structure`