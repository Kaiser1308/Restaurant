# Nhiệm vụ đang làm (Active Tasks)

Dùng để xé nhỏ `PHASE_1_TASKS.md` thành các việc chi tiết của ngày hôm nay.

## Task: Day 2 - Base Components & Backend Config
**Status:** 🚧 In Progress

### Yêu cầu
Hoàn thiện giao diện cơ bản (UI Kit) và setup Injection bên Backend.

### Checklist (Để giao cho AI)
- [ ] **Frontend:** Code `Button.tsx` (có primary/secondary variant).
- [ ] **Frontend:** Code `Input.tsx` (có trạng thái error/disabled).
- [ ] **Frontend:** Code `Card.tsx` (container cho menu/table).
- [ ] **Backend:** Setup Dependency Injection ở `Program.cs`.
- [ ] **Backend:** Thêm pattern `Result<T>` để chuẩn hóa API response.

Commit: `chore: scaffold base components, layouts, and project structure`